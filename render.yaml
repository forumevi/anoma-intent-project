services:
  - type: web
    name: intent-demo
    env: node
    repo: https://github.com/forumevi/anoma-intent-project.git
    branch: main
    buildCommand: "npm install && npm run build"
    startCommand: "npm run start"
    plan: free
